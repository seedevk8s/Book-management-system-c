# 게시판 시스템 전체 설계 문서

## 📋 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [요구사항 분석](#요구사항-분석)
3. [시스템 아키텍처](#시스템-아키텍처)
4. [데이터베이스 설계](#데이터베이스-설계)
5. [API 설계](#api-설계)
6. [화면 설계](#화면-설계)
7. [보안 설계](#보안-설계)
8. [구현 계획](#구현-계획)

## 프로젝트 개요

### 🎯 프로젝트 목표
기존 Spring Security 기반 회원 관리 시스템에 **완전한 기능의 게시판 시스템**을 통합하여, 실무에서 바로 활용 가능한 웹 애플리케이션을 구축합니다.

### 🛠️ 기술 스택
- **Backend**: Spring Boot 3.x, Spring Security 6.x, Spring Data JPA
- **Frontend**: Thymeleaf, Bootstrap 5, JavaScript (ES6+)
- **Database**: MySQL 8.0
- **Build Tool**: Gradle
- **File Storage**: 로컬 파일 시스템 (추후 클라우드 확장 가능)

### 📊 주요 특징
- **Spring Security 통합**: 기존 인증/인가 시스템과 완전 통합
- **실시간 기능**: 댓글, 추천 등 동적 기능
- **파일 관리**: 첨부파일 업로드/다운로드
- **검색 최적화**: 다양한 검색 조건 지원
- **반응형 UI**: 모바일/데스크톱 호환

## 요구사항 분석

### 1. 기능적 요구사항

#### 🔐 인증/인가 요구사항
- **게시글 조회**: 모든 사용자 (비회원 포함)
- **게시글 작성**: 로그인한 사용자만
- **게시글 수정/삭제**: 작성자 또는 관리자만
- **댓글 작성**: 로그인한 사용자만
- **댓글 수정/삭제**: 작성자 또는 관리자만
- **관리 기능**: 관리자만 (카테고리 관리, 게시글 관리)

#### 📝 게시판 핵심 기능
1. **게시글 관리**
   - 게시글 작성/수정/삭제
   - 카테고리별 분류
   - 첨부파일 업로드 (이미지, 문서)
   - 게시글 상태 관리 (공개/비공개/임시저장)

2. **조회 및 검색**
   - 페이징 처리 (10/20/50개씩)
   - 정렬 기능 (최신순, 조회수순, 추천순)
   - 통합 검색 (제목, 내용, 작성자)
   - 카테고리별 필터링
   - 기간별 검색

3. **상호작용 기능**
   - 댓글/대댓글 시스템
   - 게시글 추천/비추천
   - 조회수 증가
   - 북마크/즐겨찾기

4. **관리 기능**
   - 카테고리 관리
   - 게시글 일괄 관리
   - 댓글 관리
   - 통계 대시보드

### 2. 비기능적 요구사항

#### 🚀 성능 요구사항
- **페이지 로딩 시간**: 3초 이내
- **동시 사용자**: 100명 이상
- **파일 업로드**: 최대 10MB
- **검색 응답시간**: 1초 이내

#### 🔒 보안 요구사항
- **XSS 방지**: 모든 사용자 입력 검증
- **CSRF 보호**: 중요한 작업에 토큰 검증
- **파일 보안**: 업로드 파일 검증
- **권한 체크**: 모든 작업에 권한 검증

#### 📱 사용성 요구사항
- **반응형 디자인**: 모바일/태블릿/데스크톱 지원
- **직관적 UI**: 사용자 친화적 인터페이스
- **접근성**: 웹 표준 준수

## 시스템 아키텍처

### 📐 전체 아키텍처
```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                       │
├─────────────────────────────────────────────────────────────┤
│  Controllers  │  REST APIs  │  Thymeleaf Views  │  Static   │
│  - Board      │  - Board    │  - board/         │  - CSS    │
│  - Comment    │  - Comment  │  - comment/       │  - JS     │
│  - Category   │  - File     │  - category/      │  - Images │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                   Spring Security Layer                     │
├─────────────────────────────────────────────────────────────┤
│  Filter Chain │  Auth Providers │  Access Control │  CSRF   │
│  - Security   │  - User Details │  - @PreAuthorize│  - Token│
│  - CORS       │  - Password     │  - Method Level │  - Form │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                     Business Layer                          │
├─────────────────────────────────────────────────────────────┤
│  Services     │  DTOs          │  Mappers        │  Utils   │
│  - BoardSvc   │  - BoardDTO    │  - Entity↔DTO  │  - File  │
│  - CommentSvc │  - CommentDTO  │  - Search       │  - Valid │
│  - FileSvc    │  - CategoryDTO │  - Pagination   │  - Format│
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                  Data Access Layer                          │
├─────────────────────────────────────────────────────────────┤
│  Repositories │  Entities      │  Custom Queries │  Config  │
│  - Board      │  - Board       │  - Search       │  - JPA   │
│  - Comment    │  - Comment     │  - Statistics   │  - DB    │
│  - Category   │  - Category    │  - Pagination   │  - Pool  │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│                     Infrastructure                          │
├─────────────────────────────────────────────────────────────┤
│  Database     │  File System   │  Cache          │  Logging │
│  - MySQL      │  - Local FS    │  - Spring Cache │  - Logback│
│  - Connection │  - Upload Dir  │  - Redis(옵션)  │  - SLF4J │
│  - Pool       │  - Temp Dir    │  - EhCache      │  - 파일   │
└─────────────────────────────────────────────────────────────┘
```

### 🔄 요청 처리 플로우
```
User Request → Security Filter → Controller → Service → Repository → Database
     ↓              ↓               ↓           ↓          ↓          ↓
Response ← View Template ← Model ← DTO ← Entity ← Query Result
```

## 데이터베이스 설계

### 📊 ERD 설계

#### 핵심 테이블 구조

**1. boards (게시판 테이블)**
```sql
CREATE TABLE boards (
    board_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    member_id BIGINT NOT NULL,
    category_id BIGINT,
    view_count INT DEFAULT 0,
    like_count INT DEFAULT 0,
    comment_count INT DEFAULT 0,
    status ENUM('PUBLISHED', 'DRAFT', 'DELETED') DEFAULT 'PUBLISHED',
    is_notice BOOLEAN DEFAULT FALSE,
    is_pinned BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at DATETIME NULL,
    
    FOREIGN KEY (member_id) REFERENCES members(member_id),
    FOREIGN KEY (category_id) REFERENCES board_categories(category_id),
    
    INDEX idx_member_id (member_id),
    INDEX idx_category_id (category_id),
    INDEX idx_created_at (created_at),
    INDEX idx_status (status),
    INDEX idx_is_notice (is_notice),
    FULLTEXT INDEX ft_title_content (title, content)
);
```

**2. board_categories (카테고리 테이블)**
```sql
CREATE TABLE board_categories (
    category_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE,
    description TEXT,
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**3. board_comments (댓글 테이블)**
```sql
CREATE TABLE board_comments (
    comment_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    board_id BIGINT NOT NULL,
    member_id BIGINT NOT NULL,
    parent_id BIGINT NULL,
    content TEXT NOT NULL,
    depth INT DEFAULT 0,
    sort_order INT DEFAULT 0,
    is_deleted BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    FOREIGN KEY (board_id) REFERENCES boards(board_id),
    FOREIGN KEY (member_id) REFERENCES members(member_id),
    FOREIGN KEY (parent_id) REFERENCES board_comments(comment_id),
    
    INDEX idx_board_id (board_id),
    INDEX idx_member_id (member_id),
    INDEX idx_parent_id (parent_id),
    INDEX idx_created_at (created_at)
);
```

**4. board_files (첨부파일 테이블)**
```sql
CREATE TABLE board_files (
    file_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    board_id BIGINT NOT NULL,
    original_name VARCHAR(255) NOT NULL,
    stored_name VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) NOT NULL,
    file_size BIGINT NOT NULL,
    content_type VARCHAR(100),
    download_count INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (board_id) REFERENCES boards(board_id),
    INDEX idx_board_id (board_id)
);
```

**5. board_likes (추천 테이블)**
```sql
CREATE TABLE board_likes (
    like_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    board_id BIGINT NOT NULL,
    member_id BIGINT NOT NULL,
    like_type ENUM('LIKE', 'DISLIKE') NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (board_id) REFERENCES boards(board_id),
    FOREIGN KEY (member_id) REFERENCES members(member_id),
    UNIQUE KEY uk_board_member (board_id, member_id),
    INDEX idx_board_id (board_id),
    INDEX idx_member_id (member_id)
);
```

**6. board_views (조회 기록 테이블)**
```sql
CREATE TABLE board_views (
    view_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    board_id BIGINT NOT NULL,
    member_id BIGINT NULL,
    ip_address VARCHAR(45),
    user_agent TEXT,
    viewed_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (board_id) REFERENCES boards(board_id),
    FOREIGN KEY (member_id) REFERENCES members(member_id),
    INDEX idx_board_id (board_id),
    INDEX idx_member_id (member_id),
    INDEX idx_ip_address (ip_address),
    INDEX idx_viewed_at (viewed_at)
);
```

### 🔗 엔티티 관계

```
Member ||--o{ Board : "작성"
Member ||--o{ Comment : "작성"
Member ||--o{ Like : "추천/비추천"
Member ||--o{ View : "조회"

Board ||--o{ Comment : "댓글"
Board ||--o{ File : "첨부파일"
Board ||--o{ Like : "추천/비추천"
Board ||--o{ View : "조회"
Board }o--|| Category : "분류"

Comment ||--o{ Comment : "대댓글" (self-reference)
```

## API 설계

### 🌐 REST API 엔드포인트

#### 게시판 API
```
GET    /api/boards                    # 게시글 목록 조회
GET    /api/boards/{id}               # 게시글 상세 조회
POST   /api/boards                    # 게시글 작성
PUT    /api/boards/{id}               # 게시글 수정
DELETE /api/boards/{id}               # 게시글 삭제
POST   /api/boards/{id}/like          # 게시글 추천
DELETE /api/boards/{id}/like          # 게시글 추천 취소
GET    /api/boards/search             # 게시글 검색
```

#### 댓글 API
```
GET    /api/boards/{boardId}/comments    # 댓글 목록 조회
POST   /api/boards/{boardId}/comments    # 댓글 작성
PUT    /api/comments/{id}                # 댓글 수정
DELETE /api/comments/{id}               # 댓글 삭제
```

#### 파일 API
```
POST   /api/boards/{boardId}/files      # 파일 업로드
GET    /api/files/{id}/download         # 파일 다운로드
DELETE /api/files/{id}                  # 파일 삭제
```

#### 카테고리 API
```
GET    /api/categories                  # 카테고리 목록
POST   /api/categories                  # 카테고리 생성 (관리자)
PUT    /api/categories/{id}             # 카테고리 수정 (관리자)
DELETE /api/categories/{id}             # 카테고리 삭제 (관리자)
```

### 📝 요청/응답 스키마

#### 게시글 작성 요청
```json
{
  "title": "게시글 제목",
  "content": "게시글 내용",
  "categoryId": 1,
  "status": "PUBLISHED",
  "isNotice": false,
  "files": ["fileId1", "fileId2"]
}
```

#### 게시글 목록 응답
```json
{
  "content": [
    {
      "id": 1,
      "title": "게시글 제목",
      "authorName": "작성자",
      "categoryName": "공지사항",
      "viewCount": 100,
      "likeCount": 10,
      "commentCount": 5,
      "createdAt": "2024-01-01T10:00:00",
      "isNotice": false,
      "isPinned": false
    }
  ],
  "pageable": {
    "page": 0,
    "size": 10,
    "sort": "createdAt,desc"
  },
  "totalElements": 150,
  "totalPages": 15,
  "first": true,
  "last": false
}
```

## 화면 설계

### 📱 화면 구성

#### 1. 게시판 메인 페이지 (`/boards`)
```
┌─────────────────────────────────────────────────────────┐
│ [로고]          [검색박스]              [로그인]        │
├─────────────────────────────────────────────────────────┤
│ [전체] [공지] [자유] [질문] [기타]                      │
├─────────────────────────────────────────────────────────┤
│ 📌 [공지] 중요한 공지사항입니다         👤admin  📅today│
│ 📌 [공지] 시스템 점검 안내             👤admin  📅today│
├─────────────────────────────────────────────────────────┤
│ 1. [자유] 안녕하세요 첫 글입니다        👤user1  📅1시간│
│ 2. [질문] Spring Boot 질문 있습니다    👤user2  📅2시간│
│ 3. [자유] 오늘 날씨가 좋네요           👤user3  📅3시간│
├─────────────────────────────────────────────────────────┤
│ [이전] 1 2 3 4 5 ... 10 [다음]        [글쓰기] [관리] │
└─────────────────────────────────────────────────────────┘
```

#### 2. 게시글 상세 페이지 (`/boards/{id}`)
```
┌─────────────────────────────────────────────────────────┐
│ [뒤로가기] > 자유게시판 > 게시글 제목                   │
├─────────────────────────────────────────────────────────┤
│ 제목: 안녕하세요 첫 글입니다                            │
│ 작성자: user1 | 조회: 100 | 추천: 10 | 2024-01-01      │
├─────────────────────────────────────────────────────────┤
│ 게시글 내용이 여기에 표시됩니다.                        │
│                                                         │
│ 첨부파일: 📎 image.jpg (다운로드: 5)                   │
├─────────────────────────────────────────────────────────┤
│ [👍 추천 10] [👎 비추천 2] [💾 저장] [📢 신고]        │
├─────────────────────────────────────────────────────────┤
│ 💬 댓글 (3)                            [수정] [삭제]   │
│ ├─ user2: 좋은 글 감사합니다 (1시간 전)               │
│ ├─ user3: 도움이 되었어요 (2시간 전)                  │
│ │  └─ user1: 감사합니다 (1시간 전)                   │
│ └─ [댓글 작성] [내용 입력란] [등록]                    │
└─────────────────────────────────────────────────────────┘
```

#### 3. 게시글 작성 페이지 (`/boards/write`)
```
┌─────────────────────────────────────────────────────────┐
│ 게시글 작성                                [취소] [임시저장] [발행] │
├─────────────────────────────────────────────────────────┤
│ 카테고리: [선택 ▼] 제목: [_________________]            │
├─────────────────────────────────────────────────────────┤
│ 내용:                                                   │
│ ┌─ [B] [I] [U] [링크] [이미지] [파일] ──────────────┐  │
│ │                                                 │  │
│ │ [에디터 영역]                                   │  │
│ │                                                 │  │
│ └─────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│ 첨부파일: [파일 선택] [업로드]                         │
│ 📎 uploaded_file.pdf (삭제)                            │
├─────────────────────────────────────────────────────────┤
│ 옵션: ☐ 공지글 ☐ 댓글 허용 ☐ 비공개                  │
└─────────────────────────────────────────────────────────┘
```

### 🎨 UI/UX 가이드라인

#### 색상 시스템
- **Primary**: #007bff (파란색) - 주요 버튼, 링크
- **Secondary**: #6c757d (회색) - 보조 텍스트
- **Success**: #28a745 (녹색) - 성공 메시지, 추천
- **Danger**: #dc3545 (빨강) - 경고, 삭제, 비추천
- **Warning**: #ffc107 (노랑) - 주의, 알림
- **Info**: #17a2b8 (청록) - 정보 표시

#### 타이포그래피
- **제목**: 18px, Bold, #333
- **본문**: 14px, Regular, #666
- **작은 텍스트**: 12px, Regular, #999
- **링크**: 14px, Regular, #007bff

#### 반응형 디자인
- **Desktop**: 1200px 이상 - 3단 레이아웃
- **Tablet**: 768px ~ 1199px - 2단 레이아웃
- **Mobile**: 767px 이하 - 1단 레이아웃

## 보안 설계

### 🔒 보안 정책

#### 1. 인증/인가
```java
// 게시판 접근 권한
@PreAuthorize("permitAll()")                    // 게시글 조회
@PreAuthorize("isAuthenticated()")              // 게시글 작성
@PreAuthorize("@boardService.isOwnerOrAdmin(#id, authentication.name)")  // 수정/삭제
@PreAuthorize("hasRole('ADMIN')")               // 관리 기능
```

#### 2. 데이터 검증
```java
// 입력 데이터 검증
@Valid @NotBlank @Size(min=1, max=200)          // 제목
@Valid @NotBlank @Size(min=1, max=10000)        // 내용
@Valid @Pattern(regexp="^[\\w가-힣\\s]+$")        // 카테고리명
```

#### 3. XSS 방지
- 모든 사용자 입력 HTML 이스케이프
- 허용된 HTML 태그만 사용 (화이트리스트)
- CSP (Content Security Policy) 헤더 설정

#### 4. 파일 업로드 보안
```java
// 허용 파일 타입
private static final Set<String> ALLOWED_TYPES = Set.of(
    "image/jpeg", "image/png", "image/gif",
    "application/pdf", "application/msword",
    "text/plain"
);

// 파일 크기 제한: 10MB
private static final long MAX_FILE_SIZE = 10 * 1024 * 1024;
```

## 구현 계획

### 📅 개발 일정 (총 2주)

#### 1주차: 핵심 기능 구현
**Day 1-2: 기반 설정**
- [ ] 엔티티 클래스 생성
- [ ] Repository 인터페이스 구현
- [ ] 기본 Service 클래스 구현

**Day 3-4: 게시판 CRUD**
- [ ] BoardController 구현
- [ ] 게시글 목록/상세 조회
- [ ] 게시글 작성/수정/삭제
- [ ] 페이징 및 정렬

**Day 5-7: 기본 화면**
- [ ] Thymeleaf 템플릿 작성
- [ ] Bootstrap 스타일링
- [ ] 기본 JavaScript 기능

#### 2주차: 고급 기능 구현
**Day 8-9: 댓글 시스템**
- [ ] 댓글 CRUD API
- [ ] 대댓글 처리
- [ ] 실시간 댓글 업데이트

**Day 10-11: 파일 및 검색**
- [ ] 파일 업로드/다운로드
- [ ] 통합 검색 기능
- [ ] 카테고리 관리

**Day 12-14: 완성 및 최적화**
- [ ] 추천/조회수 기능
- [ ] 권한 체크 완성
- [ ] 성능 최적화
- [ ] 테스트 및 버그 수정

### 🏗️ 구현 순서

#### Phase 1: 데이터 계층
1. **엔티티 생성**: Board, Comment, Category, File, Like, View
2. **Repository 구현**: JPA Repository + Custom Query
3. **DTO 클래스**: 요청/응답 객체 정의

#### Phase 2: 비즈니스 계층
1. **Service 구현**: BoardService, CommentService, FileService
2. **Mapper 구현**: Entity ↔ DTO 변환
3. **유틸리티**: 파일 처리, 검색, 페이징

#### Phase 3: 프레젠테이션 계층
1. **Controller 구현**: REST API + View Controller
2. **템플릿 작성**: Thymeleaf 화면
3. **JavaScript**: 동적 기능 구현

#### Phase 4: 보안 및 최적화
1. **권한 설정**: 메서드 레벨 보안
2. **성능 최적화**: 쿼리 최적화, 캐싱
3. **테스트**: 단위/통합 테스트

### 📂 패키지 구조
```
src/main/java/com/library/
├── board/
│   ├── controller/
│   │   ├── BoardController.java
│   │   ├── BoardApiController.java
│   │   └── CommentApiController.java
│   ├── service/
│   │   ├── BoardService.java
│   │   ├── CommentService.java
│   │   ├── CategoryService.java
│   │   └── FileService.java
│   ├── repository/
│   │   ├── BoardRepository.java
│   │   ├── CommentRepository.java
│   │   ├── CategoryRepository.java
│   │   └── FileRepository.java
│   ├── entity/
│   │   ├── Board.java
│   │   ├── Comment.java
│   │   ├── Category.java
│   │   ├── BoardFile.java
│   │   ├── BoardLike.java
│   │   └── BoardView.java
│   ├── dto/
│   │   ├── request/
│   │   ├── response/
│   │   └── search/
│   └── util/
│       ├── FileUtil.java
│       ├── SearchUtil.java
│       └── PageUtil.java
└── config/
    ├── BoardConfig.java
    └── FileConfig.java
```

### 🧪 테스트 계획
- **단위 테스트**: Service, Repository 계층
- **통합 테스트**: Controller, API 엔드포인트
- **UI 테스트**: Selenium 기반 E2E 테스트
- **성능 테스트**: JMeter 부하 테스트

---

## 🎯 마무리

이 설계 문서는 **완전한 기능의 게시판 시스템**을 구현하기 위한 상세한 로드맵입니다. 

### 📈 예상 결과물
- **실무 수준의 게시판**: 기업에서 바로 사용 가능한 품질
- **확장 가능한 아키텍처**: 새로운 기능 추가 용이
- **보안 강화**: Spring Security 완전 통합
- **사용자 친화적**: 직관적이고 반응형 UI

이제 이 설계를 바탕으로 단계별 구현을 시작하겠습니다! 🚀
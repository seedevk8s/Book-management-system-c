# Spring Security 학습 가이드 - 실습 과제

## 🎯 학습 목표
이 프로젝트를 통해 **Spring Security**의 핵심 개념과 실무 적용 방법을 완전히 이해하고 활용할 수 있게 됩니다.

## 📚 단계별 학습 계획

### 1단계: 기본 개념 이해 (1-2일)

#### 🔍 분석할 주요 파일들
1. **SecurityConfig.java** - Spring Security 설정의 핵심
2. **Member.java** - UserDetails 구현체 분석
3. **CustomUserDetailsService.java** - 인증 서비스 이해

#### 📝 학습 과제
- [ ] Spring Security Filter Chain의 동작 순서 파악
- [ ] UserDetails와 UserDetailsService의 역할 이해
- [ ] BCrypt 암호화 방식의 장점 분석
- [ ] 세션 기반 인증 vs JWT 토큰 인증 비교

#### 💡 핵심 질문
1. 왜 Member 엔티티가 UserDetails를 구현하는가?
2. DaoAuthenticationProvider는 어떤 역할을 하는가?
3. 로그인 시 Spring Security는 어떤 순서로 검증하는가?

### 2단계: 인증 플로우 분석 (2-3일)

#### 🎯 분석 포인트
- 로그인 요청이 들어왔을 때 전체 처리 과정
- 성공/실패 핸들러의 역할과 커스터마이징
- 세션 관리와 보안 고려사항

#### 🛠️ 실습 과제
1. **로그 분석 실습**
   ```bash
   # 애플리케이션 실행 후 로그인 시도
   # 각 단계별 로그 메시지 확인
   ```

2. **핸들러 커스터마이징**
   - 로그인 실패 횟수 제한 기능 추가
   - 로그인 성공 시 마지막 접속 시간 업데이트

3. **보안 설정 실험**
   - CSRF 활성화/비활성화 차이점 확인
   - 세션 타임아웃 설정 변경

### 3단계: 권한 관리 시스템 (3-4일)

#### 📊 권한 구조 분석
```
Role Hierarchy:
├── ADMIN (관리자)
│   ├── 모든 권한
│   └── 사용자 관리
├── LIBRARIAN (사서)
│   ├── 도서 관리
│   └── 대출/반납 관리
└── USER (일반 사용자)
    ├── 도서 조회
    └── 자신의 대출 내역 조회
```

#### 🔨 실습 과제
1. **URL 기반 권한 제어 확장**
   ```java
   .requestMatchers("/admin/**").hasRole("ADMIN")
   .requestMatchers("/librarian/**").hasAnyRole("ADMIN", "LIBRARIAN")
   .requestMatchers("/books/manage/**").hasAnyRole("ADMIN", "LIBRARIAN")
   ```

2. **메서드 기반 권한 제어**
   ```java
   @PreAuthorize("hasRole('ADMIN') or #member.email == authentication.name")
   public Member updateMember(Long id, Member member) { ... }
   ```

3. **동적 권한 확인**
   - 본인의 데이터만 접근 가능하도록 제어
   - 관리자는 모든 데이터 접근 가능

### 4단계: 고급 보안 기능 (4-5일)

#### 🔐 고급 보안 구현
1. **계정 잠금 기능**
   ```java
   // 로그인 실패 5회 시 계정 잠금
   // 관리자가 수동으로 잠금 해제 가능
   ```

2. **비밀번호 정책 강화**
   ```java
   // 최소 8자, 대소문자, 숫자, 특수문자 포함
   // 이전 비밀번호와 다른 비밀번호 사용
   ```

3. **세션 보안 강화**
   ```java
   // 동시 세션 제한
   // 세션 고정 공격 방지
   // 안전한 쿠키 설정
   ```

### 5단계: 실무 응용 (5-7일)

#### 🌟 실무 기능 구현
1. **소셜 로그인 연동**
   - Google OAuth2 연동
   - 기존 계정과 소셜 계정 연결

2. **API 보안**
   - JWT 토큰 기반 API 인증
   - CORS 설정
   - API Rate Limiting

3. **감사 로깅**
   - 모든 보안 이벤트 로깅
   - 로그인/로그아웃 기록
   - 권한 변경 기록

## 🧪 실습 환경 설정

### 개발 환경 구성
```bash
# 1. 프로젝트 클론 및 실행
git clone [repository-url]
cd security-project
./gradlew bootRun

# 2. 데이터베이스 설정 (MySQL)
# application-dev.yml 확인
# 테스트 데이터 입력

# 3. 테스트 계정
# 일반 사용자: user@test.com / password123
# 관리자: admin@test.com / password123
```

### 개발 도구 활용
- **IntelliJ IDEA**: 디버깅 및 코드 분석
- **Postman**: API 테스트
- **MySQL Workbench**: 데이터베이스 관리
- **Browser DevTools**: 네트워크 및 쿠키 분석

## 📋 체크리스트

### 기본 이해도 확인
- [ ] Spring Security의 기본 동작 원리 이해
- [ ] 인증(Authentication)과 인가(Authorization) 차이점 명확히 구분
- [ ] 세션 기반 인증의 장단점 이해
- [ ] BCrypt 암호화의 특징과 장점 이해

### 코드 분석 완료
- [ ] SecurityConfig.java의 모든 설정 이해
- [ ] 커스텀 핸들러들의 역할과 동작 방식 이해
- [ ] Member 엔티티의 UserDetails 구현 분석
- [ ] 서비스 계층의 역할과 책임 이해

### 실습 과제 완료
- [ ] 새로운 권한 추가 및 테스트
- [ ] 커스텀 핸들러 수정 및 기능 확장
- [ ] 보안 설정 변경 및 효과 확인
- [ ] 에러 처리 및 사용자 경험 개선

### 고급 기능 구현
- [ ] 계정 잠금/해제 기능
- [ ] 비밀번호 정책 강화
- [ ] 세션 관리 고도화
- [ ] 감사 로깅 시스템

## 🚀 다음 단계 학습 방향

### 1. JWT 토큰 기반 인증
```java
// Stateless 인증 방식으로 확장
// Mobile App, SPA 호환성 향상
```

### 2. OAuth2 소셜 로그인
```java
// Google, GitHub, Kakao 등 소셜 로그인
// Spring Security OAuth2 Client 활용
```

### 3. 마이크로서비스 보안
```java
// API Gateway 인증
// 서비스 간 인증/인가
// JWT 토큰 검증
```

### 4. 고급 보안 패턴
```java
// RBAC (Role-Based Access Control)
// ABAC (Attribute-Based Access Control)
// Zero Trust Architecture
```

## 📝 학습 노트 템플릿

### 일일 학습 기록
```markdown
## 날짜: 2024-XX-XX

### 오늘 학습한 내용
- [ ] 주요 개념
- [ ] 코드 분석
- [ ] 실습 내용

### 이해한 점
- 

### 어려웠던 점
- 

### 내일 학습 계획
- 
```

### 주요 개념 정리
```markdown
## Spring Security 핵심 개념

### Authentication (인증)
- 정의: 
- 구현 방법:
- 주요 클래스:

### Authorization (인가)
- 정의:
- 구현 방법:
- 주요 어노테이션:
```

## 🎯 최종 목표

이 학습 과정을 완료하면 다음과 같은 역량을 갖추게 됩니다:

1. **Spring Security 설정**을 자유자재로 다룰 수 있음
2. **인증/인가 시스템**을 처음부터 설계하고 구현할 수 있음
3. **보안 취약점**을 이해하고 대응할 수 있음
4. **실무 프로젝트**에 바로 적용할 수 있는 수준의 이해도
5. **최신 보안 트렌드**에 대한 기본기 확보

---

## 💬 학습 지원

### 질문과 답변
학습 중 궁금한 점이 있으면 다음과 같이 질문해주세요:

1. **구체적인 코드 라인** 지정
2. **어떤 부분이 이해되지 않는지** 명시
3. **시도해본 방법들** 설명
4. **예상 결과와 실제 결과** 비교

### 추가 자료
- [Spring Security 공식 문서](https://docs.spring.io/spring-security/reference/)
- [Spring Boot Security 자동 설정](https://docs.spring.io/spring-boot/docs/current/reference/html/web.html#web.security)
- [OWASP 보안 가이드](https://owasp.org/www-project-top-ten/)

**행운을 빌어요! 🍀**